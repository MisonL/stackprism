interface:
  display_name: 'StackPrism Site Experience'
  short_description: 'Evidence for website recreation'
  brand_color: '#0F766E'
  default_prompt: 'Use $stackprism-site-experience to inspect this URL and recreate a similar site from observed tech stack, UI, layout, UX, and assets.'
policy:
  allow_implicit_invocation: true
